return {
  album_type = "both",       -- "album", "single", or "both"
  min_release_year = 1950,   -- only show albums released on or after this year
  max_results = 10,          -- maximum number of albums to return
}